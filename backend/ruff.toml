target-version = "py311"

[lint.flake8-bugbear]
# FastAPI evaluates these declarations when it builds the route dependency graph.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.File", "sqlmodel.col"]
